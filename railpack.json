{
  "$schema": "https://railway.com/railpack.schema.json",
  "build": {
    "commands": {
      "install": "composer install --no-dev --optimize-autoloader --no-interaction",
      "npm": "npm install && npm run build",
      "prepare": "mkdir -p storage/framework/{sessions,views,cache,testing} storage/logs bootstrap/cache && chmod -R a+rw storage bootstrap/cache && apt-get update && apt-get install -y php8.2-gd || true",
      "optimize": "php artisan config:cache && php artisan route:cache && php artisan view:cache"
    }
  },
  "start": "php artisan migrate --force && php artisan serve --host 0.0.0.0 --port $PORT"
}
